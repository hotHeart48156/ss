#include <vector>
#include<algorithm>
namespace greedy
{
    class AssignCookie
    {
    private:
        /* data */
    public:
        AssignCookie(/* args */);
        int findContentChildren(std::vector<int> &g, std::vector<int> s);
    };

} // namespace greedy