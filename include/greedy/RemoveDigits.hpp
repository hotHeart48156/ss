#include <string>
#include<stack>
#include<vector>
namespace greedy
{
    class RemoveDigits
    {
    private:
        /* data */
    public:
        RemoveDigits(/* args */);
        std::string removeKDigits(std::string num, int k);
    };

} // namespace greedy