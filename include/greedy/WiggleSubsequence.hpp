#include <vector>
namespace greedy
{
    class WiggleSubsequence
    {
    private:
        /* data */
    public:
        WiggleSubsequence(/* args */);
        int wiggleMaxLength(std::vector<int>& nums);
    };

} // namespace greedy